language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "pypy"

# no deps required for odswriter to work, but I nose2 used for tests.

before_install:
  - sudo apt-get update -qq

install:
  - sudo apt-get install -qq libreoffice-calc
  - pip install nose2

# command to run tests
script: nose2